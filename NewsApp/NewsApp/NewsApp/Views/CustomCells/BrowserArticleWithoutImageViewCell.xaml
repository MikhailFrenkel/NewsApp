<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewsApp.Views.CustomCells.BrowserArticleWithoutImageViewCell">

    <Frame Padding="0" Margin="3" CornerRadius="10" BackgroundColor="{StaticResource NewsTitleHolder}">
        <Grid>
            <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold" TextColor="{StaticResource NewsText}"
                   VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="100" />
        </Grid>
    </Frame>
</ViewCell>