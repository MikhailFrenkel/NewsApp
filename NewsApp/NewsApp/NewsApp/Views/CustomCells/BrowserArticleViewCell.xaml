<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewsApp.Views.CustomCells.BrowserArticleViewCell">

    <!--<Frame Padding="0" Margin="2" CornerRadius="10">
        <StackLayout BackgroundColor="{StaticResource NewsTitleHolder}">
            <Frame CornerRadius="10" BackgroundColor="{StaticResource ImagePlaceholder}" Padding="0" HasShadow="False" >
                <Image Source="{Binding ImageUrl}" Aspect="AspectFill" WidthRequest="100" HeightRequest="30" />
            </Frame>

            <StackLayout Padding="5">
                <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold" TextColor="{StaticResource NewsText}"
                       VerticalOptions="FillAndExpand" WidthRequest="100"/>
            </StackLayout>
        </StackLayout>
    </Frame>-->

    <Frame Padding="0" Margin="3" CornerRadius="10">
        <Grid>
            <Image Source="{Binding ImageUrl}" Aspect="AspectFill" WidthRequest="100" HeightRequest="30" />
            <Label Text="{Binding Name}" FontSize="14" FontAttributes="Bold" TextColor="{StaticResource NewsText}"
                   VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="100"
                   Opacity="0.8"/>
        </Grid>
    </Frame>
</ViewCell>