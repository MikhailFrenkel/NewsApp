<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewsApp.Views.CustomCells.ArticleWithoutImageViewCell">

    <Frame Padding="2" Margin="7,3" CornerRadius="5">
        <StackLayout BackgroundColor="{StaticResource NewsTitleHolder}" Padding="5">
            <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource NewsText}"
                   VerticalOptions="FillAndExpand"/>
            <Label Text="{Binding DatePublished, Converter={StaticResource DateConverter}}" FontSize="14" VerticalOptions="End" />
        </StackLayout>
    </Frame>
    
</ViewCell>