<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewsApp.WorldNews"
             Title="World">

    <StackLayout>
        <ListView  x:Name="NewsListView"
                   ItemsSource="{Binding WorldNewsResult.Articles}"
                   RowHeight="60"
                   ItemSelected="ItemSelectedHandler">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal" Padding="5">
                                <Image Source="{Binding UrlToImage}" HeightRequest="50" WidthRequest="80" Aspect="AspectFill" />
                                <StackLayout>
                                    <Label Text="{Binding Title}" FontSize="14" FontAttributes="Bold" />
                                    <Label Text="{Binding PublishedAt}" FontSize="6" />
                                </StackLayout>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

    
</ContentPage>